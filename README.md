I use this to publish random things online.

To run locally, `yarn`, then `yarn dev`, then open https://localhost:8000.
___

### Todos:
- Fix 404 page on load content jumping.
- Make sure 404 page is being used.